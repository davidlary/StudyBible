---
layout: layout.njk
title: Acts Chapter 10
---

<h1>Acts Chapter 10</h1>
<p class="subtitle">Cornelius and Peter: The Gospel Opens to the Gentiles</p>

{% for verse in acts10_verses %}
<article class="verse" id="verse-{{ verse.verse_number }}">

    {# ==================== VERSE HEADER ==================== #}
    <header class="verse-header">
        <h2 class="verse-reference">Acts 10:{{ verse.verse_number }}</h2>
    </header>

    {# ==================== PRIMARY SCRIPTURE TEXT ==================== #}
    <section class="scripture-primary">
        <p class="scripture-text">{{ verse.data.section_1_sacred_text.standalone_english_translation }}</p>
    </section>

    {# ==================== INTERLINEAR ANALYSIS (Collapsible) ==================== #}
    <details class="interlinear-section" open>
        <summary class="section-toggle">
            <span class="toggle-icon">▸</span>
            <span class="toggle-label">Original Language & Interlinear Analysis</span>
        </summary>

        <div class="interlinear-content">
            {# Greek Text #}
            <div class="original-language">
                <h3 class="minor-heading">Greek Text (SBLGNT)</h3>
                <p class="greek-text">{{ verse.data.section_1_sacred_text.original_script }}</p>
            </div>

            {# Translation Comparison #}
            <div class="translation-comparison">
                <h3 class="minor-heading">Translation Comparison</h3>

                <div class="translation-row">
                    <span class="translation-label">Direct:</span>
                    <span class="translation-text">{{ verse.data.section_1_sacred_text.faithful_direct_translation }}</span>
                </div>

                <div class="translation-row">
                    <span class="translation-label">English:</span>
                    <span class="translation-text">{{ verse.data.section_1_sacred_text.standalone_english_translation }}</span>
                </div>

                <div class="translation-row">
                    <span class="translation-label">Amplified:</span>
                    <span class="translation-text">{{ verse.data.section_1_sacred_text.amplified_narrative_translation }}</span>
                </div>
            </div>

            {# Word-by-Word Interlinear Table #}
            {% if verse.data.interlinear_analysis and verse.data.interlinear_analysis.length > 0 %}
            <div class="interlinear-table">
                <table class="morphology-grid">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Greek/Hebrew</th>
                            <th>Transliteration</th>
                            <th>Parsing</th>
                            <th>Strong's</th>
                            <th>English</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for word in verse.data.interlinear_analysis %}
                        <tr>
                            <td class="word-num">{{word.word_number}}</td>
                            <td class="original-word">{{word.greek_word or word.hebrew_word}}</td>
                            <td class="translit">{{word.transliteration}}</td>
                            <td class="parsing" title="{{word.parsing_expanded}}">{{word.parsing_code}}</td>
                            <td class="strongs">{{word.strongs_number}}</td>
                            <td class="gloss">{{word.english_gloss}}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            <p class="interlinear-note">
                <em>Note: Word-by-word morphological analysis will be available when verses are regenerated with new schema.</em>
            </p>
            {% endif %}

            {# Geographic Calculations #}
            {% if verse.data.geographic_calculations and verse.data.geographic_calculations.length > 0 %}
            <div class="geo-calculations">
                <h4 class="minor-heading">Geographic Context</h4>
                {% for calc in verse.data.geographic_calculations %}
                <div class="geo-item">
                    <p><strong>{{calc.from_location}} to {{calc.to_location}}:</strong></p>
                    <ul class="geo-details">
                        <li>Straight-line: {{calc.straight_line_distance_km}} km ({{calc.straight_line_distance_mi}} mi)</li>
                        <li>Ancient route: ~{{calc.ancient_route_distance_km}} km ({{calc.ancient_route_distance_mi}} mi)</li>
                        <li>Elevation change: {{calc.elevation_change_m}}m ({{calc.elevation_change_ft}}ft)</li>
                        <li>Travel time: {{calc.estimated_travel_time_days.on_foot}} days on foot, {{calc.estimated_travel_time_days.by_horse}} day by horse</li>
                        {% if calc.uncertainty_notes %}<li class="uncertainty">Note: {{calc.uncertainty_notes}}</li>{% endif %}
                    </ul>
                </div>
                {% endfor %}
            </div>
            {% endif %}
        </div>
    </details>

    {# ==================== EXEGETICAL NOTES (Collapsible) ==================== #}
    <details class="exegesis-section" open>
        <summary class="section-toggle">
            <span class="toggle-icon">▸</span>
            <span class="toggle-label">Exegetical Notes & Commentary</span>
        </summary>

        <div class="exegesis-content">

            {# 1. Linguistic Analysis #}
            {% if verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names %}
            <div class="note-section">
                <h3 class="note-heading">Linguistic Mechanics & Etymology</h3>
                <div class="note-prose">
                {% if verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names | isArray %}
                    {% for item in verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names %}
                        {% if item.entry %}
                            <p><strong>{{item.entry}}:</strong> {{item.theological_significance}}</p>
                        {% else %}
                            <p>{{item}}</p>
                        {% endif %}
                    {% endfor %}
                {% else %}
                    <p>{{ verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names }}</p>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# 2. Literal-Primary Filter #}
            {% if verse.data.section_2_exegetical_synthesis.literal_primary_filter %}
            <div class="note-section">
                <h3 class="note-heading">Textual & Contextual Analysis</h3>
                <div class="note-prose">
                {% if verse.data.section_2_exegetical_synthesis.literal_primary_filter | isArray %}
                    {% for paragraph in verse.data.section_2_exegetical_synthesis.literal_primary_filter %}
                        <p>{{paragraph}}</p>
                    {% endfor %}
                {% else %}
                    <p>{{ verse.data.section_2_exegetical_synthesis.literal_primary_filter }}</p>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# 3. Analogia Scriptura #}
            {% if verse.data.section_2_exegetical_synthesis.aggregate_analogia_scriptura %}
            <div class="note-section">
                <h3 class="note-heading">Scripture Interpreting Scripture</h3>
                {% if verse.data.section_2_exegetical_synthesis.aggregate_analogia_scriptura | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.aggregate_analogia_scriptura | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.aggregate_analogia_scriptura }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# 4. Life Application (moved from separate section) #}
            {% if verse.data.section_3_life_application.practical_application or verse.data.section_3_life_application.application_summary %}
            <div class="note-section">
                <h3 class="note-heading">Practical Application</h3>
                {% if verse.data.section_3_life_application.practical_application %}
                    {% if verse.data.section_3_life_application.practical_application | isArray %}
                        <p class="note-prose">{{ verse.data.section_3_life_application.practical_application | join(' ') }}</p>
                    {% else %}
                        <p class="note-prose">{{ verse.data.section_3_life_application.practical_application }}</p>
                    {% endif %}
                {% elif verse.data.section_3_life_application.application_summary %}
                    {% if verse.data.section_3_life_application.application_summary | isArray %}
                        <p class="note-prose">{{ verse.data.section_3_life_application.application_summary | join(' ') }}</p>
                    {% else %}
                        <p class="note-prose">{{ verse.data.section_3_life_application.application_summary }}</p>
                    {% endif %}
                {% endif %}
            </div>
            {% endif %}

            {# 5. Prophetic Typology #}
            {% if verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality %}
            <div class="note-section">
                <h3 class="note-heading">Prophetic Typology & Intertextuality</h3>
                <div class="note-prose">
                {% set propheticData = verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality %}
                {% if propheticData.type_shadow %}
                    <p><strong>Type/Shadow:</strong> {{propheticData.type_shadow}}</p>
                {% endif %}
                {% if propheticData.antitype_fulfillment %}
                    <p><strong>Antitype/Fulfillment:</strong> {{propheticData.antitype_fulfillment}}</p>
                {% endif %}
                {% if propheticData.progression_notes %}
                    <p><strong>Progression:</strong> {{propheticData.progression_notes}}</p>
                {% endif %}
                {% if not propheticData.type_shadow and not propheticData.antitype_fulfillment %}
                    <p>{{propheticData}}</p>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# 6. Historical Context #}
            {% if verse.data.section_2_exegetical_synthesis.historical_context_and_chronology %}
            <div class="note-section">
                <h3 class="note-heading">Historical Context & Chronology</h3>
                <div class="note-prose">
                {% set historicalData = verse.data.section_2_exegetical_synthesis.historical_context_and_chronology %}
                {% if historicalData.dates %}
                    <p><strong>Dating:</strong> {{historicalData.dates}}</p>
                {% endif %}
                {% if historicalData.context %}
                    <p>{{historicalData.context}}</p>
                {% endif %}
                {% if not historicalData.dates and not historicalData.context %}
                    <p>{{historicalData}}</p>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# 7. Geospatial #}
            {% if verse.data.section_2_exegetical_synthesis.geospatial_and_physical_geography %}
            <div class="note-section">
                <h3 class="note-heading">Geography & Physical Setting</h3>
                <div class="note-prose">
                {% set geoData = verse.data.section_2_exegetical_synthesis.geospatial_and_physical_geography %}
                {% if geoData.modern_location %}
                    <p><strong>Location:</strong> {{geoData.modern_location}}</p>
                {% endif %}
                {% if geoData.coordinates %}
                    <p><strong>Coordinates:</strong> {{geoData.coordinates}}</p>
                {% endif %}
                {% if geoData.altitude_m %}
                    <p><strong>Altitude:</strong> {{geoData.altitude_m}}m</p>
                {% endif %}
                {% if geoData.terrain_climate_characteristics %}
                    <p>{{geoData.terrain_climate_characteristics}}</p>
                {% endif %}
                {% if not geoData.modern_location and not geoData.coordinates %}
                    <p>{{geoData}}</p>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# 8. Socio-Political #}
            {% if verse.data.section_2_exegetical_synthesis.socio_political_matrix %}
            <div class="note-section">
                <h3 class="note-heading">Socio-Political Context</h3>
                {% if verse.data.section_2_exegetical_synthesis.socio_political_matrix | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.socio_political_matrix | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.socio_political_matrix }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# 9. Archaeological #}
            {% if verse.data.section_2_exegetical_synthesis.archaeological_confirmation %}
            <div class="note-section">
                <h3 class="note-heading">Archaeological Evidence</h3>
                {% if verse.data.section_2_exegetical_synthesis.archaeological_confirmation | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.archaeological_confirmation | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.archaeological_confirmation }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# 10. Literary Devices #}
            {% if verse.data.section_2_exegetical_synthesis.literary_devices %}
            <div class="note-section">
                <h3 class="note-heading">Literary Structure & Devices</h3>
                {% if verse.data.section_2_exegetical_synthesis.literary_devices | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literary_devices | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literary_devices }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# 11. Numerical Significance #}
            {% if verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance %}
            <div class="note-section">
                <h3 class="note-heading">Numerical & Symbolic Significance</h3>
                {% if verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance }}</p>
                {% endif %}
            </div>
            {% endif %}

        </div>
    </details>

</article>
{% endfor %}

<style>
/* ==========================================================
   PROSE-FIRST REDESIGN: Reading Experience Optimized
   LSB-inspired clean layout, BLB-style progressive disclosure
   ========================================================== */

/* ===== VERSE CONTAINER ===== */
.verse {
    margin-bottom: 4rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid #E5E7EB;
}

.verse:last-child {
    border-bottom: none;
}

/* ===== VERSE HEADER: Minimal, Clear ===== */
.verse-header {
    margin-bottom: 1.5rem;
}

.verse-reference {
    font-family: var(--font-headings);
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--burgundy);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin: 0;
}

/* ===== PRIMARY SCRIPTURE TEXT: Prominent, Readable ===== */
.scripture-primary {
    margin-bottom: 2rem;
}

.scripture-text {
    font-family: var(--font-scripture);
    font-size: 1.3rem;
    line-height: 1.8;
    color: var(--text-primary);
    max-width: 70ch;
    margin: 0;
    font-weight: 400;
}

/* ===== COLLAPSIBLE SECTIONS: Progressive Disclosure ===== */
details {
    margin-bottom: 1.5rem;
    border: 1px solid #E5E7EB;
    border-radius: 6px;
    background: white;
}

details[open] {
    border-color: #D1D5DB;
}

summary {
    cursor: pointer;
    padding: 0.75rem 1rem;
    font-family: var(--font-commentary);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--navy);
    user-select: none;
    list-style: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: background 0.2s;
}

summary::-webkit-details-marker {
    display: none;
}

summary:hover {
    background: #F9FAFB;
}

.toggle-icon {
    transition: transform 0.2s;
    color: var(--gold);
    font-size: 0.8em;
}

details[open] .toggle-icon {
    transform: rotate(90deg);
}

.toggle-label {
    flex: 1;
}

/* ===== SECTION CONTENT: Clean, Breathable ===== */
.interlinear-content,
.exegesis-content,
.application-content {
    padding: 1.5rem;
    border-top: 1px solid #F3F4F6;
}

/* ===== INTERLINEAR SECTION ===== */
.interlinear-section {
    background: #F8FAFC;
}

.original-language {
    margin-bottom: 1.5rem;
}

.greek-text {
    font-family: var(--font-original);
    font-size: 1.3rem;
    line-height: 1.6;
    color: var(--navy);
    margin-top: 0.5rem;
}

.translation-comparison {
    margin-top: 1.5rem;
}

.translation-row {
    display: grid;
    grid-template-columns: 100px 1fr;
    gap: 1rem;
    margin-bottom: 0.75rem;
    font-size: 0.95rem;
}

.translation-label {
    font-family: var(--font-commentary);
    font-weight: 600;
    color: var(--burgundy);
    font-size: 0.85rem;
    letter-spacing: 0.03em;
}

.translation-text {
    font-family: var(--font-scripture);
    color: var(--text-secondary);
    line-height: 1.6;
}

.interlinear-note {
    margin-top: 1.5rem;
    padding: 0.75rem;
    background: #FFF7ED;
    border-left: 3px solid var(--gold);
    font-size: 0.9rem;
    color: var(--text-muted);
}

/* Morphology Table Styles */
.interlinear-table {
    margin-top: 1.5rem;
    overflow-x: auto;
}

.morphology-grid {
    width: 100%;
    border-collapse: collapse;
    font-family: var(--font-commentary);
    font-size: 0.9rem;
}

.morphology-grid th {
    background: var(--navy);
    color: white;
    padding: 0.5rem;
    text-align: left;
    font-weight: 600;
    font-size: 0.85rem;
    letter-spacing: 0.02em;
}

.morphology-grid td {
    padding: 0.5rem;
    border-bottom: 1px solid #E5E7EB;
}

.morphology-grid tr:last-child td {
    border-bottom: none;
}

.morphology-grid tr:hover {
    background: #F9FAFB;
}

.word-num {
    color: var(--text-muted);
    font-size: 0.85rem;
    width: 40px;
}

.original-word {
    font-family: var(--font-original);
    font-size: 1.1rem;
    color: var(--navy);
    font-weight: 600;
}

.translit {
    color: var(--text-secondary);
    font-style: italic;
}

.parsing {
    font-family: monospace;
    font-size: 0.85rem;
    color: var(--burgundy);
    cursor: help;
}

.strongs {
    color: var(--gold);
    font-weight: 600;
    font-size: 0.85rem;
}

.gloss {
    color: var(--text-secondary);
}

/* Geographic Calculations Styles */
.geo-calculations {
    margin-top: 1.5rem;
    padding: 1rem;
    background: #F0F9FF;
    border-radius: 6px;
}

.geo-item {
    margin-bottom: 1rem;
}

.geo-item:last-child {
    margin-bottom: 0;
}

.geo-item p {
    margin: 0 0 0.5rem 0;
    color: var(--navy);
    font-family: var(--font-commentary);
}

.geo-details {
    list-style: none;
    padding-left: 1rem;
    margin: 0;
    font-family: var(--font-commentary);
    font-size: 0.9rem;
    color: var(--text-secondary);
}

.geo-details li {
    margin-bottom: 0.25rem;
}

.geo-details .uncertainty {
    color: var(--text-muted);
    font-style: italic;
    margin-top: 0.5rem;
}

/* ===== EXEGESIS SECTION ===== */
.exegesis-section {
    background: #FFFBEB;
}

.note-section {
    margin-bottom: 2rem;
}

.note-section:last-child {
    margin-bottom: 0;
}

.minor-heading,
.note-heading {
    font-family: var(--font-headings);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--navy);
    margin-bottom: 0.75rem;
    letter-spacing: 0.02em;
}

.note-prose {
    font-family: var(--font-commentary);
    font-size: 1rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin: 0;
    text-align: justify;
    hyphens: auto;
    max-width: 70ch;
}

/* ===== APPLICATION SECTION ===== */
.application-section {
    background: #F0FDF4;
}

.application-prose {
    font-family: var(--font-commentary);
    font-size: 1rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin: 0;
    text-align: justify;
    hyphens: auto;
    max-width: 70ch;
}

/* ===== RESPONSIVE ADJUSTMENTS ===== */
@media (min-width: 1200px) {
    .scripture-text,
    .note-prose,
    .application-prose {
        max-width: 90ch;
    }
}

@media (max-width: 768px) {
    .scripture-text {
        font-size: 1.15rem;
    }

    .translation-row {
        grid-template-columns: 1fr;
        gap: 0.25rem;
    }

    .translation-label {
        margin-bottom: 0.25rem;
    }

    .note-prose,
    .application-prose {
        text-align: left;
    }
}

/* ===== PRINT OPTIMIZATION ===== */
@media print {
    details {
        border: none;
    }

    summary {
        display: none;
    }

    .interlinear-content,
    .exegesis-content,
    .application-content {
        display: block !important;
        padding: 0;
    }
}
</style>
