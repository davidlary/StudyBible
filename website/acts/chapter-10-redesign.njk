---
layout: layout.njk
title: Acts Chapter 10
---

<h1>Acts Chapter 10</h1>
<p class="subtitle">Cornelius and Peter: The Gospel Opens to the Gentiles</p>

{% for verse in acts10_verses %}
<article class="verse" id="verse-{{ verse.verse_number }}">

    {# ==================== VERSE HEADER ==================== #}
    <header class="verse-header">
        <h2 class="verse-reference">Acts 10:{{ verse.verse_number }}</h2>
    </header>

    {# ==================== PRIMARY SCRIPTURE TEXT ==================== #}
    <section class="scripture-primary">
        <p class="scripture-text">{{ verse.data.section_1_sacred_text.standalone_english_translation }}</p>
    </section>

    {# ==================== INTERLINEAR ANALYSIS (Collapsible) ==================== #}
    <details class="interlinear-section">
        <summary class="section-toggle">
            <span class="toggle-icon">▸</span>
            <span class="toggle-label">Original Language & Interlinear Analysis</span>
        </summary>

        <div class="interlinear-content">
            {# Greek Text #}
            <div class="original-language">
                <h3 class="minor-heading">Greek Text (SBLGNT)</h3>
                <p class="greek-text">{{ verse.data.section_1_sacred_text.original_script }}</p>
            </div>

            {# Translation Comparison #}
            <div class="translation-comparison">
                <h3 class="minor-heading">Translation Comparison</h3>

                <div class="translation-row">
                    <span class="translation-label">Direct:</span>
                    <span class="translation-text">{{ verse.data.section_1_sacred_text.faithful_direct_translation }}</span>
                </div>

                <div class="translation-row">
                    <span class="translation-label">Amplified:</span>
                    <span class="translation-text">{{ verse.data.section_1_sacred_text.amplified_narrative_translation }}</span>
                </div>
            </div>

            {# TODO: Add word-by-word interlinear here when morphological data available #}
            <p class="interlinear-note">
                <em>Note: Word-by-word morphological analysis with Strong's numbers will be added in a future update.</em>
            </p>
        </div>
    </details>

    {# ==================== EXEGETICAL NOTES (Collapsible) ==================== #}
    <details class="exegesis-section" open>
        <summary class="section-toggle">
            <span class="toggle-icon">▸</span>
            <span class="toggle-label">Exegetical Notes & Commentary</span>
        </summary>

        <div class="exegesis-content">

            {# Literal-Primary Filter #}
            {% if verse.data.section_2_exegetical_synthesis.literal_primary_filter %}
            <div class="note-section">
                <h3 class="note-heading">Textual & Contextual Analysis</h3>
                {% if verse.data.section_2_exegetical_synthesis.literal_primary_filter | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literal_primary_filter | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literal_primary_filter }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Prophetic Typology #}
            {% if verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality %}
            <div class="note-section">
                <h3 class="note-heading">Prophetic Typology & Intertextuality</h3>
                {% if verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.prophetic_typology_and_intertextuality }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Linguistic Analysis #}
            {% if verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names %}
            <div class="note-section">
                <h3 class="note-heading">Linguistic Mechanics & Etymology</h3>
                {% if verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.linguistic_mechanics_and_names }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Literary Devices #}
            {% if verse.data.section_2_exegetical_synthesis.literary_devices %}
            <div class="note-section">
                <h3 class="note-heading">Literary Structure & Devices</h3>
                {% if verse.data.section_2_exegetical_synthesis.literary_devices | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literary_devices | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.literary_devices }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Numerical Significance #}
            {% if verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance %}
            <div class="note-section">
                <h3 class="note-heading">Numerical & Symbolic Significance</h3>
                {% if verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.numerical_and_gematria_significance }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Historical Context #}
            {% if verse.data.section_2_exegetical_synthesis.historical_context_and_chronology %}
            <div class="note-section">
                <h3 class="note-heading">Historical Context & Chronology</h3>
                {% if verse.data.section_2_exegetical_synthesis.historical_context_and_chronology | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.historical_context_and_chronology | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.historical_context_and_chronology }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Socio-Political #}
            {% if verse.data.section_2_exegetical_synthesis.socio_political_matrix %}
            <div class="note-section">
                <h3 class="note-heading">Socio-Political Context</h3>
                {% if verse.data.section_2_exegetical_synthesis.socio_political_matrix | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.socio_political_matrix | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.socio_political_matrix }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Geospatial #}
            {% if verse.data.section_2_exegetical_synthesis.geospatial_data_and_physical_geography %}
            <div class="note-section">
                <h3 class="note-heading">Geography & Physical Setting</h3>
                {% if verse.data.section_2_exegetical_synthesis.geospatial_data_and_physical_geography | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.geospatial_data_and_physical_geography | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.geospatial_data_and_physical_geography }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Archaeological #}
            {% if verse.data.section_2_exegetical_synthesis.archaeological_confirmation %}
            <div class="note-section">
                <h3 class="note-heading">Archaeological Evidence</h3>
                {% if verse.data.section_2_exegetical_synthesis.archaeological_confirmation | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.archaeological_confirmation | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.archaeological_confirmation }}</p>
                {% endif %}
            </div>
            {% endif %}

            {# Analogia Scriptura #}
            {% if verse.data.section_2_exegetical_synthesis.aggregate_perspective_and_analogia_scriptura %}
            <div class="note-section">
                <h3 class="note-heading">Scripture Interpreting Scripture</h3>
                {% if verse.data.section_2_exegetical_synthesis.aggregate_perspective_and_analogia_scriptura | isArray %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.aggregate_perspective_and_analogia_scriptura | join(' ') }}</p>
                {% else %}
                    <p class="note-prose">{{ verse.data.section_2_exegetical_synthesis.aggregate_perspective_and_analogia_scriptura }}</p>
                {% endif %}
            </div>
            {% endif %}

        </div>
    </details>

    {# ==================== LIFE APPLICATION (Collapsible) ==================== #}
    <details class="application-section">
        <summary class="section-toggle">
            <span class="toggle-icon">▸</span>
            <span class="toggle-label">Practical Application</span>
        </summary>

        <div class="application-content">
            {% if verse.data.section_3_life_application.practical_application %}
                {% if verse.data.section_3_life_application.practical_application | isArray %}
                    <p class="application-prose">{{ verse.data.section_3_life_application.practical_application | join(' ') }}</p>
                {% else %}
                    <p class="application-prose">{{ verse.data.section_3_life_application.practical_application }}</p>
                {% endif %}
            {% elif verse.data.section_3_life_application.application_summary %}
                {% if verse.data.section_3_life_application.application_summary | isArray %}
                    <p class="application-prose">{{ verse.data.section_3_life_application.application_summary | join(' ') }}</p>
                {% else %}
                    <p class="application-prose">{{ verse.data.section_3_life_application.application_summary }}</p>
                {% endif %}
            {% endif %}
        </div>
    </details>

</article>
{% endfor %}

<style>
/* ==========================================================
   PROSE-FIRST REDESIGN: Reading Experience Optimized
   LSB-inspired clean layout, BLB-style progressive disclosure
   ========================================================== */

/* ===== VERSE CONTAINER ===== */
.verse {
    margin-bottom: 4rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid #E5E7EB;
}

.verse:last-child {
    border-bottom: none;
}

/* ===== VERSE HEADER: Minimal, Clear ===== */
.verse-header {
    margin-bottom: 1.5rem;
}

.verse-reference {
    font-family: var(--font-headings);
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--burgundy);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin: 0;
}

/* ===== PRIMARY SCRIPTURE TEXT: Prominent, Readable ===== */
.scripture-primary {
    margin-bottom: 2rem;
}

.scripture-text {
    font-family: var(--font-scripture);
    font-size: 1.3rem;
    line-height: 1.8;
    color: var(--text-primary);
    max-width: 70ch;
    margin: 0;
    font-weight: 400;
}

/* ===== COLLAPSIBLE SECTIONS: Progressive Disclosure ===== */
details {
    margin-bottom: 1.5rem;
    border: 1px solid #E5E7EB;
    border-radius: 6px;
    background: white;
}

details[open] {
    border-color: #D1D5DB;
}

summary {
    cursor: pointer;
    padding: 0.75rem 1rem;
    font-family: var(--font-commentary);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--navy);
    user-select: none;
    list-style: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: background 0.2s;
}

summary::-webkit-details-marker {
    display: none;
}

summary:hover {
    background: #F9FAFB;
}

.toggle-icon {
    transition: transform 0.2s;
    color: var(--gold);
    font-size: 0.8em;
}

details[open] .toggle-icon {
    transform: rotate(90deg);
}

.toggle-label {
    flex: 1;
}

/* ===== SECTION CONTENT: Clean, Breathable ===== */
.interlinear-content,
.exegesis-content,
.application-content {
    padding: 1.5rem;
    border-top: 1px solid #F3F4F6;
}

/* ===== INTERLINEAR SECTION ===== */
.interlinear-section {
    background: #F8FAFC;
}

.original-language {
    margin-bottom: 1.5rem;
}

.greek-text {
    font-family: var(--font-original);
    font-size: 1.3rem;
    line-height: 1.6;
    color: var(--navy);
    margin-top: 0.5rem;
}

.translation-comparison {
    margin-top: 1.5rem;
}

.translation-row {
    display: grid;
    grid-template-columns: 100px 1fr;
    gap: 1rem;
    margin-bottom: 0.75rem;
    font-size: 0.95rem;
}

.translation-label {
    font-family: var(--font-commentary);
    font-weight: 600;
    color: var(--burgundy);
    font-size: 0.85rem;
    letter-spacing: 0.03em;
}

.translation-text {
    font-family: var(--font-scripture);
    color: var(--text-secondary);
    line-height: 1.6;
}

.interlinear-note {
    margin-top: 1.5rem;
    padding: 0.75rem;
    background: #FFF7ED;
    border-left: 3px solid var(--gold);
    font-size: 0.9rem;
    color: var(--text-muted);
}

/* ===== EXEGESIS SECTION ===== */
.exegesis-section {
    background: #FFFBEB;
}

.note-section {
    margin-bottom: 2rem;
}

.note-section:last-child {
    margin-bottom: 0;
}

.minor-heading,
.note-heading {
    font-family: var(--font-headings);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--navy);
    margin-bottom: 0.75rem;
    letter-spacing: 0.02em;
}

.note-prose {
    font-family: var(--font-commentary);
    font-size: 1rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin: 0;
    text-align: justify;
    hyphens: auto;
    max-width: 70ch;
}

/* ===== APPLICATION SECTION ===== */
.application-section {
    background: #F0FDF4;
}

.application-prose {
    font-family: var(--font-commentary);
    font-size: 1rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin: 0;
    text-align: justify;
    hyphens: auto;
    max-width: 70ch;
}

/* ===== RESPONSIVE ADJUSTMENTS ===== */
@media (max-width: 768px) {
    .scripture-text {
        font-size: 1.15rem;
    }

    .translation-row {
        grid-template-columns: 1fr;
        gap: 0.25rem;
    }

    .translation-label {
        margin-bottom: 0.25rem;
    }

    .note-prose,
    .application-prose {
        text-align: left;
    }
}

/* ===== PRINT OPTIMIZATION ===== */
@media print {
    details {
        border: none;
    }

    summary {
        display: none;
    }

    .interlinear-content,
    .exegesis-content,
    .application-content {
        display: block !important;
        padding: 0;
    }
}
</style>
